spring:
  datasource:
    url: jdbc:mysql://localhost:3306/knowledge_server?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: your_local_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password:

jwt:
  secret: replace-with-your-local-jwt-secret
  expiration: 7200000

logging:
  level:
    root: INFO
    com.codersole.knowledgeserver: DEBUG
